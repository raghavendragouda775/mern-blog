{
  "name": "blog_m",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "express": "^4.21.0",
    "mongoose": "^8.7.0",
    "dotenv": "^16.4.5",
    "concurrently": "^7.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon index.js",
    "client": "npm run dev --prefix client",
 "build": "npm install && npm install --prefix client && cd client && npx vite build"
  },
  "engines": {
  "node": ">=16.0.0"
}
}

